const initialState = {

};
export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    // case ActionConst.SOME_ACTION:
    //   return {
    //     ...state,
    //   };

    default:
      return state;
  }
}
